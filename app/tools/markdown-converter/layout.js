export const metadata = {
  title: 'Markdown to HTML Converter - Convert Markdown to HTML',
  description: 'Convert Markdown text to HTML with live preview.',
}

export default function Layout({ children }) {
  return children;
}
